# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Secrets / env
.env
.env.*
!.env.example

# Wallet/keys (never commit)
*.key
keys.txt
wallets.json
wallets.encrypted.json
# Testnet burner wallets & local test state (contain private keys / ephemeral data)
.testnet-wallet*.json
.burner.json
.testnet-contract.json
.fastmint-contracts.json

# Scheduler state
data/*
!data/.gitkeep

# Logs & OS
*.log
.DS_Store

# Alchemy signup password (local only)
.alchemy-account.txt
